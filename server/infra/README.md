# infra

개발 환경, 배포 환경, 모니터링 관련 설정을 둡니다.

## 하위 폴더

- `docker/`: 로컬에서 API·DB·Redis를 함께 실행하는 설정
- `terraform/`: 클라우드 인프라 생성 설정
- `monitoring/`: 오류 추적·로그·알림 설정

## 이후 구현

- 개발·스테이징·운영 환경 분리
- 비밀 키와 환경 변수 관리
- DB 백업과 파일 보관 기간 설정
