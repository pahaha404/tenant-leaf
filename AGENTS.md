# 세입세잎 작업 규칙

이 파일은 팀원과 AI 코딩 도구가 프로젝트에서 따라야 하는 공통 규칙입니다.

## 프로젝트 구조와 담당 경계

- `android/`: Android 프런트엔드, 화면, 권한, 사용자 확인 흐름
- `server/api/`: 인증, 매물·임장, JPEG 미디어 상태와 관찰 결과를 관리하는 API
- `server/ai-worker/`: 비동기 JPEG 구역 분류와 확인 필요 관찰 후보 생성. STT는 후순위이며 리포트 생성 시점은 계약 확정 전까지 가정하지 않음
- `server/shared-types/`: 앱·서버·AI 사이의 공통 OpenAPI 계약
- `ai/ml/`: 학습·평가 코드와 데이터셋 메타데이터. 원본 이미지는 저장하지 않음
- `team/`: 공통 계약과 역할별 시작 안내·작업 순서
- `server/backendmds/`: 서버·Android 연동 관련 결정, 구현 요청서와 완료 기록
- `server/technical/`: 서버 구조와 데이터 모델 문서
- `공용 Task.md`, `android/android/Task.md`, `server/backend/Task.md`: 공통·역할별 작업 추적
- `design/`: 디자인 산출물

## 절대 지켜야 하는 서비스 규칙

- Meta 안경은 서버와 직접 통신하지 않는다. 흐름은 `안경 -> Android 앱 -> API -> AI 작업자`다.
- Android 앱은 AI 제공자 API 키를 저장하거나 직접 호출하지 않는다.
- AI는 하자 또는 안전 상태를 확정하지 않는다. AI 출력은 근거 미디어가 있는 `확인 필요 관찰 결과`다.
- MVP 현장 점검에는 체크리스트 상태 입력을 사용하지 않는다. AI가 만든 관찰의 사용자 검토 상태는 사용자의 Android 앱 행동으로만 변경한다.
- 원본 영상은 휴대전화 갤러리에만 두고 서버에는 업로드하지 않는다. 서버 분석 입력은 계약에 맞는 JPEG만 허용한다.
- 공통 요청·응답 또는 상태값을 바꾸면 관련 계약 문서와 구현을 같은 변경에서 수정한다.
- 실제 촬영 사진, 영상, 음성, 주소, GPS, 계정 정보, API 키는 GitHub에 올리지 않는다.

## Git 작업 규칙

- `main`: 발표·데모 가능한 안정 버전만 둔다.
- `develop`: 팀 통합 브랜치다.
- 기능 작업은 `feature/<영역>-<기능>` 브랜치에서 한다. 예: `feature/api-visit-session`.
- `main`과 `develop`에 직접 푸시하지 않는다. 기능 브랜치에서 `develop`으로 Pull Request를 만든다.
- 다른 사람의 진행 중인 파일을 임의로 삭제·되돌리거나 `git push --force`하지 않는다.
- 커밋 전에는 `git status`와 변경 내용을 확인한다. 관련 파일 또는 폴더만 `git add`한다.
- 파일명에 사람 이름이나 이니셜을 넣지 않는다. 역할 중심 이름을 사용한다.
  - 좋은 예: `VisitSessionController.kt`, `GlassConnectionManager.kt`
  - 나쁜 예: `HSController.kt`, `MinaGlass.kt`

## Pull Request 규칙

- PR 제목은 `feat(api): 방문 세션 생성 API`처럼 `type(scope): 설명` 형식을 쓴다.
- PR에는 변경 내용, API·데이터 계약 변경 여부, 테스트 방법, 남은 위험을 작성한다.
- 실제 Meta 안경 또는 실제 AI 연동을 검증하지 못했으면 `실기기 미검증` 또는 `AI 연동 미검증`을 명시한다.
- 공통 계약, 안경 연동, 데이터셋·모델 변경은 담당자 한 명 이상에게 검토를 요청한다.

## 작업 추적 규칙

- 작업 시작 전과 종료 전 `공용 Task.md`와 해당 역할의 Task 문서를 확인한다.
- Android 세부 작업은 `android/android/Task.md`, 서버 세부 작업은 `server/backend/Task.md`에서 관리한다.
- 구현하거나 수정하기 전에는 작업 대상과 가장 가까운 요청서·계약 문서의 범위와 완료 기준을 확인한다.
- 실제 구현과 검증이 끝난 작업만 `[x]`로 표시한다.
- 완료 처리 시 관련 Task 문서의 완료 기록에 날짜, 결과, 확인 방법을 남긴다.
- 구현만 했고 실행·검증하지 못했다면 `[ ]`로 유지하고, 미검증 사유를 적는다.
- `[ ]`는 미시작·진행 중·미검증, `[x]`는 구현과 필요한 검증까지 끝난 상태다.

## AI 학습 데이터 규칙

- 원본 이미지·영상·음성·라벨 파일·학습 모델 가중치는 GitHub에 커밋하지 않는다.
- 데이터는 적법한 수집 근거와 학습 사용 범위를 확인한 뒤, 접근 제어된 저장소에 보관한다.
- 얼굴, 사람 목소리, 문서, 주소, 차량 번호, GPS 등 식별 가능 정보는 학습 전 제거 또는 가명·익명 처리한다.
- 데이터셋 버전, 출처, 동의·사용 근거, 라벨 기준, 분할 기준, 삭제 기한은 데이터셋 메타데이터로 기록한다.
- 평가 데이터는 학습 데이터와 주택 또는 촬영 장소 단위로 분리한다. 같은 집의 사진이 학습과 평가에 함께 들어가면 안 된다.
- 모델 결과에는 모델 버전, 근거 이미지 ID, 신뢰도, 사람 확인 상태를 남긴다.
